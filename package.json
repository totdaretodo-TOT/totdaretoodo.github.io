{
  "name": "yibubu-houchang",
  "version": "1.0.0",
  "private": true,
  "description": "后场一步步 Web/PWA 应用",
  "scripts": {
    "start": "python -m http.server 4173",
    "check": "node --check scripts/platform.js && node --check tasks.js && node --check app.js",
    "test": "npm run check"
  }
}
